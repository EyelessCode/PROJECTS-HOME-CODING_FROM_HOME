export class IdStudent{
    value:number

    constructor(value:number) {
        this.value=value
    }

    private validationNumber() {
        if (this.value<=0) {
            throw new 
        }
    }
}
