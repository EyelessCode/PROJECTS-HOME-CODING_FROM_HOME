// import { listEstudiantes } from './BD.js';

/* export function calculcarCalificaciones() {
    let suma=0;
    let index=0;

    for (let key in listEstudiantes.) {
        if(key.startsWith('calificacion')){
            suma+=estudiante[key];
            index++;
        }
    }

    return {suma,index};
} */

export function calcularPromedio(suma) {
    return suma / 6;
}

export function calculcarCalificaciones(z, x, c, v, b, n) {
    return (z + x + c + v + b + n);
};

/* export function calcularPromedio(s) {
    
}; */